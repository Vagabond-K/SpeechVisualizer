<Window x:Class="SpeechVisualizer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SpeechVisualizer"
        mc:Ignorable="d"
        SizeToContent="WidthAndHeight"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        Title="Speech Visualizer"
        d:DataContext="{d:DesignInstance Type=local:MainViewModel, IsDesignTimeCreatable=False}" Icon="/Images/Icon.png">
    <Grid Width="200" Height="200">
        <Viewbox Visibility="Hidden">
            <Grid x:Name="cameraView" Width="{Binding CameraWidth}" Height="{Binding CameraHeight}" Background="Black" RenderTransformOrigin="0.5,0.5" ClipToBounds="True">
                <ContentPresenter Content="{Binding Background}" Focusable="False" FocusVisualStyle="{x:Null}"/>
                <Viewbox Margin="100">
                    <ContentPresenter Content="{Binding SpeakerInCamera}" Focusable="False" FocusVisualStyle="{x:Null}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Viewbox>
            </Grid>
        </Viewbox>
        <Viewbox>
            <ContentPresenter Content="{Binding Speaker}" Focusable="False" FocusVisualStyle="{x:Null}"/>
        </Viewbox>
    </Grid>
</Window>
